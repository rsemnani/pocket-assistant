<!-- Keep PRs scoped to one issue. -->

Closes #

## What changed

-

## How it was verified

- [ ] Backend: `ruff` + `mypy` + `bandit` + `pytest` green
- [ ] Android: builds (`./gradlew :app:assembleDebug`) / tested on device
- [ ] No secrets, real hostnames/IPs, or sensitive data added

## Notes / follow-ups

<!-- Adjacent work you found but intentionally did NOT do (file as new issues). -->
